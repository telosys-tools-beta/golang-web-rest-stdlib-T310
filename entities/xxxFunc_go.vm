// Created on $today.date ( Time $today.time )
// Generated by $generator.name ( version $generator.version )
$env.setLanguage('GO')
#set($entityName = $fn.capitalize(${entity.name}) )
package entities

import 	(
	"sort"
)

func New${entityName}() $entityName {
	// new $entityName with default values ( 'zero values' )
	return $entityName{}
}

// Default sort 
func Sort${entityName}(entities []${entityName}) {
	sort.Slice(entities, func(i, j int) bool {
		// Replace Xxx by the desired field
		//return entities[i].Xxx < entities[j].Xxx
		return true
	})
}

// Specific sort (replace Xxx by the desired field)
func Sort${entityName}ByXxx(entities []${entityName}) {
	sort.Slice(entities, func(i, j int) bool {
		//return entities[i].Xxx < entities[j].Xxx
		return true
	})
}
