// Created on $today.date ( Time $today.time )
// Generated by $generator.name ( version $generator.version )
#set( $env.language = 'GO' )
package rest

func InitRestControllers() {

	apiRoot := "/api/v1" 
	
	// Mapping URI paths - controllers 
#foreach ( $entity in $selectedEntities )
	DefineRestController(apiRoot + "/$fn.toLowerCase(${entity.name})", New$fn.capitalize(${entity.name})RestController() )
#end
}
